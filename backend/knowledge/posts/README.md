# Writing templates

Add posts here using filenames such as `2026-09-01-example-post.md`. Give each post YAML front
matter for `title`, `dek`, `tag`, and `date`. Use `draft: true` until it is ready to publish.

Posts are shown on the writing pages but are not placed directly into the assistant prompt.

